name: Global CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  check-pre-commit:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Run Pre-Commit
        uses: pre-commit/action@v3.0.0
