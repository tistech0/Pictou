version: '3.1'

services:
  db:
    image: postgres
    restart: always
    volumes:
    - ./database:/var/lib/postgresql
    env_file:
      - ./back/.env
    ports:
        - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - '8080:8080'
